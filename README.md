# Blue Labs Learning

Homepage mockup for **Blue Labs Learning** — 1:1 math & STEM tutoring.

## Open locally

```bash
cd /Users/sarah/developer/BL-Learning
python3 -m http.server 5173
```

Then visit `http://localhost:5173`.

## Assets

Hand-drawn emblems live in `assets/` as transparent PNGs (including `emblem-ops-square.png`, a 2×2 stack of +, −, ×, ÷).
